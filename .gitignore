__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
*.egg-info/
dist/
build/
dist-bin/
build-pyinstaller/
*.spec
.venv/
venv/
*.db
.hypothesis/
.benchmarks/

# Credentials. Never commit these.
# `.env.example` IS tracked (it is a template with no real values); every
# other .env variant is ignored. Razorpay keys, webhook secrets, and LLM API
# keys live here and nowhere else.
.env
.env.*
!.env.example
*.pem
*.key
secrets.json

# Local demo/scratch output
demo-out/
*.evidence.json

# Local recording script -- author's notes, not part of the submission
RECORDING.txt
