# Traceability matrix

Spec section -> normative MUST -> covering test(s). Generated by `scripts/traceability.py` from the hand-curated MUST list in that file cross-referenced against `@spec("X.Y")` markers in `tests/` and `conformance/tests/`. Do not hand-edit; regenerate instead.

| Spec § | MUST | Covering test(s) |
|---|---|---|
| 3 | Every numbered lifecycle stage MUST emit its ledger event, even on denial/error. | `tests/proxy/test_lifecycle.py::test_deny_verdict_blocks_execution_and_never_calls_the_executor` |
| 4.2 | `reversible` contracts MUST declare an `undo` block that fully negates the effects. | `tests/contracts/test_model.py::test_reversible_without_undo_is_contract_invalid` |
| 4.2 | `irreversible` contracts MUST NOT declare an `undo` block. | `tests/contracts/test_model.py::test_irreversible_with_undo_is_invalid` |
| 4.2 | `conditional` contracts MUST include `undo` and `conditions`; unmet conditions at execution MUST be recorded as irreversible. | `tests/contracts/test_model.py::test_conditional_requires_undo_and_conditions` |
| 4.3 | Implementations MUST reject any construct outside the expression grammar (no calls, no dunder, no eval/exec). | `tests/contracts/test_expressions.py::test_rejects_out_of_grammar_expressions` |
| 4.4 | The `capture` call MUST be read-only. | `tests/executor/test_capture.py::test_non_read_only_capture_contract_is_rejected` |
| 4.4 | The `capture` call MUST execute before the main call, within the same step. | `tests/executor/test_capture.py::test_capture_runs_before_the_tool_call_and_snapshot_is_in_the_ledger` |
| 4.5 | With `idempotency_key` declared, a repeat call with the same key MUST return the recorded result without re-calling the tool. | `tests/executor/test_idempotency.py::test_repeating_the_same_idempotency_key_calls_upstream_once` |
| 4.6 | A tool with no contract and no `readOnlyHint` MUST be refused with `contract_missing`. | `tests/proxy/test_lifecycle.py::test_no_contract_and_no_read_only_hint_is_contract_missing` |
| 4.6 | `unsafe_passthrough` MUST be recorded in every affected ledger event. | `tests/proxy/test_lifecycle.py::test_unsafe_passthrough_call_passes_through_and_every_event_carries_override` |
| 4.7 | A session MUST pin the `set_hash` present at `session_started`; later contract changes do not apply mid-session. | `tests/proxy/test_lifecycle.py::test_session_fixes_set_hash_and_later_contract_changes_do_not_apply` |
| 5.3 | Implementations MUST NOT present `contract`-basis counts as exact. | `tests/planner/test_planner.py::test_contract_basis_plan_marks_declared_counts_as_estimates` |
| 5.4 | Executing against a prior `plan_id` MUST re-validate that args are byte-identical (else `plan_mismatch`). | `tests/planner/test_planner.py::test_plan_mismatch_on_non_identical_args` |
| 5.4 | Expired plans MUST be re-planned (`plan_expired` on stale execution). | `tests/planner/test_planner.py::test_plan_expiration_rejects_execution_after_ttl` |
| 6.2 | Every policy verdict MUST be recorded with the rule ids that fired. | `tests/policy/test_engine.py::test_cap_exceeded_produces_its_own_verdict_and_rule_id` |
| 6.3 | Caps MUST be evaluated against the upper bound for `unknown`/`estimate` effects (worst-case). | `tests/policy/test_engine.py::test_property_unknown_effects_never_resolve_to_allow_when_default_is_pause` |
| 7.1 | An expired approval item MUST NOT be executable. | `tests/approvals/test_queue.py::test_expired_item_is_never_executable_via_approve` |
| 7.2 | The approving principal MUST be authenticated by the embedding system and MUST be recorded (`approved_by`). | `tests/approvals/test_queue.py::test_approve_transitions_pending_to_approved` |
| 7.2 | An agent MUST NOT be able to approve its own actions through any tool Belay exposes. | `tests/proxy/test_no_self_approval.py::test_calling_any_approval_shaped_tool_name_is_refused_not_approved` |
| 8.1 | On recovery, Belay MUST reconcile a journaled-but-unresolved step via the idempotency key, or mark it `indeterminate`. | `tests/executor/test_recovery.py::test_recovery_reconciles_via_idempotency_key` |
| 9.2 | A `verify` operation MUST recompute the hash chain and cross-check step coherence (journal/capture/result/compensation). | `tests/ledger/test_verify.py::test_verify_coherence_flags_committed_without_journal` |
| 9.3 | Implementations MUST support field-level redaction at write time, declared in contracts. | `tests/ledger/test_redact.py::test_redacted_field_never_appears_in_cleartext` |
| 9.4 | Given the ledger alone, an implementation MUST be able to reconstruct session state, verdicts, and the compensation set. | `tests/ledger/test_replay.py::test_replay_reconstructs_basic_session_state` |
| 10.1 | Rewind of a live session MUST first fence the session (no new steps). | `tests/rewind/test_service.py::test_new_step_after_fence_raises_session_fenced` |
| 10.2 | Declared `verification` blocks MUST be checked and recorded during rewind. | `tests/rewind/test_service.py::test_verification_is_executed_and_a_failure_does_not_count_as_compensated` |
| 10.3 | A rewind result MUST enumerate steps compensated, skipped, irreversible, and indeterminate. | `tests/rewind/test_service.py::test_dry_run_enumerates_honestly_and_touches_nothing` |
| 10.3 | Implementations MUST NOT report a session "fully rewound" unless every in-scope step was compensated with passing verification. | `tests/rewind/test_service.py::test_honesty_mixed_reversible_and_irreversible_never_reports_fully_rewound` |
| 12 | Approval, policy-relaxation, and contract-edit surfaces MUST NOT be exposed as tools to the protected agent. | `tests/proxy/test_no_self_approval.py::test_agent_facing_tool_list_never_advertises_an_approval_tool` |
| 12 | Approval UIs MUST display the plan actually bound to the approval (`plan_id`), not a paraphrase. | `tests/approvals/test_queue.py::test_approval_item_is_bound_to_its_plan_id_and_replanning_invalidates_it` |
| 14 | Unknown fields MUST be preserved in ledger events (evidence is tolerant). | `tests/ledger/test_unknown_fields.py::test_unknown_payload_fields_survive_a_write_read_roundtrip` |
| 14 | Unknown fields MUST be rejected in contracts and policies (authority is strict). | `tests/contracts/test_model.py::test_unknown_top_level_field_is_rejected`<br>`tests/policy/test_engine.py::test_unknown_top_level_field_in_policy_doc_is_rejected` |

