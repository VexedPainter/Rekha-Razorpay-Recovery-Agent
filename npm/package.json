{
  "name": "belay-mcp",
  "version": "0.2.0-alpha.1",
  "description": "Belay: safe, reversible tool execution for AI agents. npm wrapper around the belay-mcp PyPI package -- lets `npx belay-mcp` and MCP-client `npx` configs work without a manual pip install.",
  "license": "MIT",
  "homepage": "https://github.com/belay-mcp/belay",
  "repository": {
    "type": "git",
    "url": "https://github.com/belay-mcp/belay"
  },
  "keywords": ["mcp", "agents", "safety", "contracts", "saga", "rollback"],
  "bin": {
    "belay": "bin/belay.js"
  },
  "scripts": {
    "postinstall": "node scripts/postinstall.js"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "bin",
    "scripts"
  ]
}
